{
  "name": "nova",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "(cd ./frontend && npm run build) && (cd ./api && npm run build)",
    "start": "concurrently \"npm run start --workspace api\" \"npm run start --workspace frontend\"",
    "dev:frontend": "npm run start --workspace frontend",
    "dev:backend": "npm run start --workspace api",
    "lint": "npm run lint --workspace api && npm run lint --workspace frontend",
    "clean": "rm -rf ./**/*dist && rm -rf node_modules && rm -rf ./**/node_modules"
  },
  "workspaces": [
    "api",
    "frontend"
  ],
  "private": true
}
