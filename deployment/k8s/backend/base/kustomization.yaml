resources:
  - service-account.yaml
  - deployment.yaml
  - service.yaml
  - istio/allow-ingress-authorization-policy.yaml

labels:
  - pairs:
      app: nova-backend
      env: ENV_PLACEHOLDER
      app.kubernetes.io/name: nova-backend
