import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#3670b3',
        },
        secondary: {
            main: '#3670b3',
        },
        divider: '#1c1c1c',
    },
});

export default theme;
