import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#001f3f',
        },
        secondary: {
            main: '#00CC00',
        },
        divider: '#1c1c1c',
    },
});

export default theme;
